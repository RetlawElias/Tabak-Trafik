With the current usage of Modules and future usage of .json files a live-Server is needed to display the current state of the project in your browser.

Thus it is recommended to either start an Apache sequence via software like XAMPP.
Or installing a VSCode-Extension called "Live Server" and pressing "Go Live" in the bottom right to launch.
